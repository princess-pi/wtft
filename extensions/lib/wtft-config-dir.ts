/** Config lives in ~/.config/wtft (`$XDG_CONFIG_HOME/wtft`). Pass as `dirName` to every `@princess-pi/libs/config` call. */
export const WTFT_CONFIG_DIR = "wtft";

/** Settings file: `wtft/config.json`. */
export const WTFT_CONFIG_TOOL = "config";
