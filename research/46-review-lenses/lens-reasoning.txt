You are a REASONING reviewer for a pull request. The branch is `46-install-wtft` in the git
repo at your current working directory; its base is `origin/main`.

Read `git diff origin/main..HEAD` and then the files it touches. Judge the ARGUMENTS the
code and its comments make, not the syntax.

Specifically hunt for: absolute claims that are only conditionally true; measurements cited
without a way to reproduce them; a comment that argues for a design the code does not
implement; a justification that would equally justify the opposite choice; a claim of
"stronger"/"nothing is lost"/"cannot happen" that has an unstated exception; and any place
the reasoning depends on a fact about this one host rather than a general property.

One line per finding, with severity, the quoted claim, path:line, and the exception or the
missing evidence. Do not triage. Do not fix anything.
